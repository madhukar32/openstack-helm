{{- if .Values.manifests.job_issu_gen_conf }}
{{- $envAll := . }}
{{- $dependencies := .Values.dependencies.issu_gen }}
{{- $mounts_issu_gen_init := .Values.pod.mounts.issu_gen.init_container }}
  {{- $suffix_obj_name := .Values.conf.suffix_obj_name | default "" }}
apiVersion: batch/v1
kind: Job
metadata:
  name: job-contrail-issu-gen-conf{{ $suffix_obj_name }}
spec:
  template:
    metadata:
      labels:
{{ tuple $envAll "opencontrail" "issu-gen-conf" | include "helm-toolkit.snippets.kubernetes_metadata_labels" | indent 8 }}
    spec:
      nodeSelector:
        {{ .Values.labels.issu_gen.node_selector_key }}: {{ .Values.labels.issu_gen.node_selector_value }}
      tolerations:
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: NoSchedule
      hostNetwork: true
      restartPolicy: OnFailure
      initContainers:
{{ tuple $envAll $dependencies $mounts_issu_gen_init | include "helm-toolkit.snippets.kubernetes_entrypoint_init_container" | indent 8 }}
      containers:
      - name: issu-gen
        image: {{ .Values.images.tags.issu_gen_conf | quote }}
        imagePullPolicy: {{ default "" .Values.images.imagePullPolicy | quote }}
        command:
        - /tmp/contrail-issu.sh
        - generate-conf
        - {{ .Values.conf.controller_node.username }}
        - {{ .Values.conf.controller_node.password }}
        # - sleep
        # - 100000s
        volumeMounts:
        - mountPath: /root/.kube/config
          name: kube-config
        - mountPath: /usr/bin/kubectl
          name: bin-kubectl
        - name: contrail-issu
          mountPath: /tmp/contrail-issu.sh
          subPath: contrail-issu.sh
      volumes:
      - name: contrail-issu
        configMap:
          name: controller-bin{{ $suffix_obj_name }}
          defaultMode: 0777
      - name: kube-config
        hostPath:
          path: /etc/kubernetes/admin.conf
      - name: bin-kubectl
        hostPath:
          path: /usr/bin/kubectl
{{- end }}
